# JetScope Environment Configuration
# Copy this file to apps/api/.env for API development, or export values in
# your shell before running npm scripts.
# NEVER commit .env to git.
# This example is public-safe and contains placeholders only.

# ============================================
# Database
# ============================================
# SQLite path for development / production-style local fallback.
JETSCOPE_DATABASE_URL=sqlite:///./data/market.db
JETSCOPE_SQLITE_PATH=./data/market.db

# ============================================
# Security
# ============================================
# Admin token for /v1/market/refresh and protected endpoints.
# Generate a strong random string for real local testing:
#   openssl rand -hex 32
JETSCOPE_ADMIN_TOKEN=REPLACE_WITH_STRONG_RANDOM_TOKEN

# ============================================
# Application
# ============================================
JETSCOPE_APP_ENV=development
JETSCOPE_API_PREFIX=/v1
JETSCOPE_WORKSPACE_SLUG=default

# Market data refresh interval (seconds).
# 600 = 10 minutes. Set to 0 to disable auto-refresh.
JETSCOPE_MARKET_REFRESH_INTERVAL_SECONDS=600

# Per-public-source HTTP timeout for synchronous market refreshes.
# Keep this bounded so admin-triggered refreshes degrade to explicit fallback
# evidence instead of hanging behind a web proxy timeout. Raise it only when you
# are deliberately validating slower live public sources.
JETSCOPE_MARKET_SOURCE_TIMEOUT_SECONDS=12

# IEA Oil Market Report credential for jet fuel stock-days coverage.
# Leave blank for the quickstart path unless you are testing live source access.
IEA_API_KEY=

# Enable legacy SQLite routes (development only).
JETSCOPE_ENABLE_SQLITE_ROUTES=false

# ============================================
# Frontend (build-time)
# ============================================
# API base URL for SSR / server-side fetch.
# In local dev: http://127.0.0.1:8000
JETSCOPE_API_BASE_URL=http://127.0.0.1:8000

# ============================================
# AI research (optional, mock-first by default)
# ============================================
# Keep these defaults for reviewer quickstart/demo runs. They require no secrets
# and avoid live external LLM calls.
# For launch readiness, set JETSCOPE_AI_RESEARCH_ENABLED=true and either keep
# mock mode for a non-production rehearsal or provide live extractor credentials
# with JETSCOPE_AI_RESEARCH_MOCK_MODE=false.
JETSCOPE_AI_RESEARCH_ENABLED=false
JETSCOPE_AI_RESEARCH_MOCK_MODE=true

# Optional live-research credentials. Leave blank for the quickstart/demo path.
# JETSCOPE_ANTHROPIC_API_KEY=
# JETSCOPE_NEWSAPI_KEY=

# ============================================
# Alert Thresholds (optional)
# ============================================
# Jet fuel price alert threshold (USD/L)
# JETSCOPE_ALERT_JET_PRICE_USD_PER_L=1.30

# Brent daily change alert threshold (%)
# JETSCOPE_ALERT_BRENT_DAILY_CHANGE_PCT=5.0
