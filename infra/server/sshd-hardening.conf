# JetScope shared-host SSH baseline.
# Install as /etc/ssh/sshd_config.d/99-jetscope-hardening.conf, then run
# "sshd -t" before a graceful reload.
X11Forwarding no
ClientAliveInterval 300
ClientAliveCountMax 2
